# Autonomous Loop — copy to `.env` and fill in. Never commit your real `.env`.
# Every value below is a placeholder; nothing here ties to any account.

AUTONOMOUS_LOOP_TASKS=   # path to a JSON task list for the CLI shell runner; blank = examples/tasks.example.json
AUTONOMOUS_LOOP_DATA_DIR=   # optional override for the ./data directory; blank = <repo>/data
